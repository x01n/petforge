# Desktop pet UI overrides

优先于 `MASTER.md` 中与桌宠浮层冲突的条目。

## Hierarchy

1. Live2D / PNG 角色本体
2. 回复气泡栈（可超过 3 条，旧条更透；不合并多分段回复）
3. 聊天输入面板
4. 右键菜单 / 托盘菜单
5. 模态确认

## Overlay placement

- 所有以桌宠为锚点弹出的浮层（输入面板、状态面板、菜单窗口、时间线/尺寸对话框）必须先检测屏幕边框：位置统一由 `meapet/desktop/screen_geometry.py` 计算，保证完整落在锚点所在屏幕的可用区域（已排除任务栏）内。
- 首选方位放不下时按 `calculate_popup_position` 的退让顺序换边，四边都放不下再夹进安全区；窗口比屏幕还大时对齐安全区左上角。
- 屏幕判定以桌宠所在屏幕为准，不得写死主屏，否则多显示器下会弹到另一块屏外面。
- 用户拖动过的浮层保留其位置，只在再次打开且已越界（换屏幕、改分辨率）时拉回可见区域。
- 分辨率 / 显示器变化后，桌宠本体必须自动回到可视范围：屏幕信号去抖合并，桌宠宽高露出不足一半时夹回可用区域，随后同步气泡与仍然打开的浮层；压在任务栏上或轻微贴边视为仍可见，不做挪动。
- 桌宠拖动结束及正常退出时把左上角位置写入配置文件同目录的 `pet_window_state.json`；启动时优先恢复，首次运行才放到主屏右下角。Live2D 必须在首帧确定最终窗口尺寸后再次恢复并夹回当前屏幕，状态不混入 `config.json`。

## Message dialogs

- 通用信息、警告、错误和确认使用 `meapet/message_dialog.py`，完整自绘标题区与圆角外壳，不显示平台原生白色标题栏。
- 保留 `QMessageBox.StandardButton` 返回值供业务判断；`Escape`、关闭按钮和窗口关闭请求必须返回当前可用的安全取消项。
- 默认按钮代表推荐的安全动作；危险动作使用危险色并保留明确文字，不以颜色作为唯一含义。
- 正文允许选择复制；长错误信息在正文区域内部滚动，窗口始终夹在锚点屏幕的可用范围内。

## Window size

- 桌宠窗口大小按百分比调节，唯一真实来源是 `display.size_factor`（30%–300%）：右键菜单「显示与立绘 · 窗口大小」提供档位与「自定义…」实时预览对话框，配置页提供同一数值的滑块。
- PNG 缩放以窗口底部中心为锚点；Live2D 缩放和视觉视口变化以完整画布上的可配置站立锚点为准。两者都在变化后夹回屏幕可用区域，放大不得把桌宠推出屏幕。
- 保存配置后运行时立即按新比例缩放，不需要重启桌宠。

## Reply presentation

- TTS 关闭：分段文字随流式增量持续增长，完成后定稿。
- TTS 开启：音频就绪后再同时显示气泡并播放；气泡始终至少比音频多保留 500ms，旧同步开关不得破坏该约束。
- TTS 失败或语言不支持：立即回退文字，不阻塞后续分段。
- 点击绑定了轮次的气泡可查看「本轮完整回复」；右键菜单提供最近对话时间线。
- Agent 工具只展示经过清洗的开始、完成、失败状态，不显示原始工具名、参数和结果。

## Session and backend state

- 直连与 Agent 一次只启用一个；保存配置会取消旧 generation 并热切换，不自动兜底。
- 时间线标签应能区分模型服务、Agent 类型与会话，缓存按作用域隔离。
- Agent 新会话保留旧时间线只读；直连清除记忆同时清除直连时间线。

## Capture consent

- 每个截图请求都在本机单独确认，授权不复用；默认范围无人操作时超时取消，用户手动改选范围后关闭本次倒计时并等待明确决定。
- 默认全屏；确认窗口允许改选区域或应用，本次结果不写磁盘。
- 确认窗口按当前范围自适应高度；全屏模式不为已隐藏的区域/应用表单保留空白占位。
- 云端路径必须同时满足 `watcher.allow_cloud`。

## Context menu

- 桌宠右键菜单是独立顶层窗口（`meapet/desktop/menu_window.py`），不是 popup：失焦不自动关闭，可拖动标题栏或空白处移动到任意位置。
- 菜单结构与文案仍由 `_build_context_menu()` 的 `QMenu` 定义；根窗口只显示第一层，分组按需在侧边打开独立级联面板，不向下撑高根菜单。靠近屏幕边缘时自动换向，更深层目录继承当前展开方向。
- 关闭方式：关闭按钮、Esc、在菜单上右键；点击任一菜单项后自动关闭并执行动作。
- 托盘菜单保持原生 `QMenu`。

## Icons

系统操作使用 `meapet/desktop/icons.py` 的 Qt 标准图标；emoji 仅作心情点缀。

## Standby recovery

- 待机时托盘提示「点击可恢复」
- 托盘单击/菜单「取消待机并显示」可唤醒
- 右键菜单仍可取消待机
- 待机开启点击穿透（Win / Linux X11）：左键等操作落到下层窗口；仅右键打开菜单
- 待机气泡不拦截鼠标；Wayland 等无原生穿透时至少禁用桌宠互动

## Motion

`display.reduced_motion` + 环境变量 +（Linux）系统动画关闭启发式 → `MEAPET_REDUCED_MOTION`。
