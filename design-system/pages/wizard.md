# Configuration wizard overrides

## Progressive disclosure

1. 环境 + 回复后端优先；模型服务商直连与 Agent 互斥
2. 语音总开关默认可用；引擎详细设置默认折叠
3. 屏幕识图默认关；先选关闭 / 继承 / 中转，再显示相应高级项
4. Hermes / OpenClaw 的 Companion MCP 仅在 Agent 模式并显式启用后显示；
   自定义 Agent Link 通过同一 WebSocket 自动交换前端工具，不显示第二套监听设置

## Backend

- 直连页明确选择 Ollama Chat、OpenAI Chat、OpenAI Responses 或 Anthropic Messages，不把 OpenAI-compatible 端点称为 Agent。
- 直连协议、地址、模型与 Key 只有一组可编辑控件；切换服务商时分别保留各自未保存草稿。
- 共用直连表单跟随当前服务商，直接显示在所选服务商说明下方；下拉框忽略滚轮改值。
- 回复随机性和最大回复长度写入实际请求；新配置最大回复长度默认 4096 tokens。
- Agent 页支持 Hermes、OpenClaw 与自定义 Agent Link，恢复上次会话；新会话由「清除记忆」确认流程创建。
- 自定义 Agent Link 的一条出站 WebSocket 同时承载聊天、主动消息和前端工具调用；
  后端必须遵循 `docs/agent-link-v1.md`，不能只实现任意 WebSocket 消息收发。
- 本地时间线默认 5 轮，可设 0–100；帮助文案说明按后端和 Agent 会话隔离。
- 配置保存后运行中热应用。失败时使用中性系统文案，不泄露响应正文。

## Companion MCP

- 默认回环地址；局域网必须填写本机具体 IP 与唯一允许的 Agent IP。
- 无证书的局域网 HTTP 需要显式勾选，并显示持续风险说明。
- Token 默认遮蔽，提供查看、复制、重新生成；重新生成后旧 Token 立即失效。
- 不自动修改 Windows 防火墙，界面只提供说明。

## TTS references

- GPT-SoVITS 参考音频按语言配置，每种语言一条固定音频并明确显示语言。
- “优先模型输出目标语朗读”会核对 `voice_language`、`voice_text` 与配置目标语，确认不一致时使用非 LLM 机器翻译回落。
- “输出语言不受支持时翻译”是独立开关；两条路径都不是模型或 TTS 请求失败兜底，单段翻译总计最多请求 3 次。

## Vision

- `inherit` 必须明确确认主回复模型支持图片。
- `relay` 在直连模式使用独立 Ollama / MiMo；Agent 模式只继承 Agent 视觉能力或关闭。
- 截图确认明确写出“本次有效”，默认全屏并允许改选区域/应用。
- 区域范围使用覆盖虚拟桌面的鼠标拖选层，不要求用户输入坐标；应用范围列出 Windows 当前可见、未最小化的窗口及所属进程，不要求记忆标题。
- 云端观察在每次截图前使用 5 秒确认框选择范围；打开范围下拉期间暂停倒计时，未改选并关闭下拉后继续；一旦用户手动更改截图方式，本次倒计时直接关闭并从界面消失，等待用户明确允许或取消；配置页不持久化本次截图范围。

## Display

- 配置中心窗口必须允许自由缩放：四条边和四个角都提供至少 10px 的命中带、显示与方向对应的缩放光标，并通过窗口系统的原生 resize 流程拖动；不得把右下角尺寸手柄作为唯一入口。自定义顶栏保留文字明确的“最大化 / 还原窗口”按钮，双击顶栏执行同一切换；最大化时禁用边缘缩放并清除顶层圆角 mask。
- 配置中心把 Live2D 作为独立顶层标签页；环境页只承载字体、桌宠整体尺寸、减少动画与环境检测，避免高级视口编辑把基础设置拉成长页。
- 字体缩放在配置页即时预览，桌宠重启后完整应用。
- 桌宠窗口大小写入 `display.size_factor`，滑块以百分比呈现（30%–300%，步长 5%），保存后由运行时立即缩放，不需要重启；桌宠右键菜单的档位与「自定义…」对话框写的是同一个值。
- Live2D 窗口范围编辑器调整 `live2d.window_mask` 对应的矩形视觉视口，并通过 `live2d.placement_anchor` 校准完整画布上的模型站立点；可选的 `live2d.window_shape` 使用多个保留多边形和挖空多边形定义静态窗口区域。完整 OpenGL 画布、模型缩放和头部/左右互动坐标保持不变。运行中的桌宠提供一次性完整帧预览；预览不可用时仍保留百分比数值输入。
- 视口支持拖动、八方向缩放手柄、整条边命中、方向键移动和四边数值输入；站立锚点十字与矩形边界同时可见并可直接拖动，不使用单独编辑模式。两者重叠时锚点优先命中，下边界其余位置仍可拖动；方向键移动最后直接操作的对象。交互目标实际命中范围至少 44×44px，状态不能只靠遮罩颜色表达。保存后立即热应用，并让完整画布上的站立锚点在缩放或裁剪变化时保持屏幕位置。
- 自定义窗口形状支持多个分离保留区、凹多边形和多个挖空区，可表达带孔洞或类似标点的分离形状；关闭开关时保留轮廓。主显示页只保留启用开关、轮廓摘要和“编辑精细形状…”入口，高级工具放入独立、可缩放且可最大化的编辑对话框，避免用户在长页面中来回滚动。
- 精细形状对话框支持按住鼠标自由套索并在松开时自动完成，也支持单击逐点绘制、双击或 Enter 完成；Backspace 撤销一点，Escape 取消当前草稿。保留/挖空工具持续选中，可连续绘制多个区域；当前工具和轮廓数量必须用文字说明，不能只靠颜色。对话框使用临时副本，“取消”不修改主配置，“应用形状”才写回并自动启用精细形状。
- 窗口形状只改变静态 OS 窗口与鼠标命中区域，不重定义头部/左右互动和语音触发分区；无有效保留区时回退普通矩形窗口，避免模型消失。
- 减少动画写入 `display.reduced_motion`，保存后由运行时立即应用。
- Python 3.10+ 可运行桌宠核心；Python 3.13+ 只提示本地 VITS 推荐使用 3.10–3.12，不得标成桌宠环境缺失。
- 数字输入框与下拉框必须使用暗色输入主题和内置高对比箭头，不显示平台原生白色步进按钮。

## Persistence and validation

- 配置页跟随桌宠当前使用的配置路径；从模板启动时保存到同目录 `config.json`。
- 保存采用字段补丁：只覆盖页面实际负责的字段；Live2D 页只修改 `live2d.window_mask`、`live2d.placement_anchor` 与 `live2d.window_shape`，Live2D 模型路径及其他 Live2D 扩展字段、角色、气泡时长和未知扩展字段原样保留。
- 配置中心把最后一次普通窗口的位置、尺寸及最大化状态写入配置文件同目录的 `wizard_window_state.json`；最大化尺寸不得覆盖普通尺寸。启动时恢复并在显示器布局变化后夹回当前可用屏幕，不写入 `config.json`，也不触发配置热更新。
- 缺少必要配置时允许用户明确确认后保存；直接关闭包含未保存编辑的窗口时先确认是否丢弃。
- 环境检测、对话、语音和识图的缺项都会同步到标签红点与顶部文字状态。
- 环境与本地服务探测不得阻塞 GUI；对话、Agent、TTS、识图均提供后台连接测试和文字结果。
- 保存成功、保存失败、缺项确认和放弃修改统一使用 `meapet/message_dialog.py` 的无边框主题消息窗口，不使用带平台原生标题栏的 `QMessageBox`；长文可复制并在窗口内滚动。
