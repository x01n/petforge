# 基础运行依赖（按需安装）
# pip install -r linux_requirements.txt
#
# 注意：
# - 推荐 Python 3.10 ~ 3.12（预编译 wheel 最全）
# - Python 3.13+ 需 numpy 2.x（1.x 无官方 wheel，会尝试源码编译并失败）
# - 本地 VITS 仍要求 numpy<2，见 vits_requirements.txt（请用 3.10~3.12）

numpy>=1.21,<2.0; python_version < "3.13"
numpy>=2.0; python_version >= "3.13"
pillow>=10.0.0
PyOpenGL>=3.1.0
PyQt5>=5.15.9
requests>=2.28.0

# 可选：Live2D 渲染（未装则自动回退 PNG）
# live2d-py

# Windows 可选
# pywin32
httpx>=0.27.0
jieba>=0.42
translators>=6.0.4,<7

# Agent 模式（Hermes/OpenClaw）与 Companion MCP 服务所需
# （与 pyproject.toml 的 dependencies 保持一致，缺失会在启用时 ImportError）
websockets>=13,<16
cryptography>=42
mcp>=1.27,<2
uvicorn>=0.30
